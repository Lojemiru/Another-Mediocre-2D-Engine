Two folders go here: output, and pages.

output needs to have the following structure:

output
   - enums
       - sprites

pages needs to have the following structure:

pages
   - pageName1
   - pageName2
   - ...

where each subfolder contains all the aseprite files for that texturepage.